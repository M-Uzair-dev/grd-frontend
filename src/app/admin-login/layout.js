export const metadata = {
  title: 'Admin Login | GRD',
  description: 'Secure login portal for GRD system administrators.',
}
 
export default function AdminLoginLayout({ children }) {
  return children;
} 