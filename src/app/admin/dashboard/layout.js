export const metadata = {
  title: 'Admin Dashboard | GRD',
  description: 'Admin dashboard for managing partners, customers, units, and reports in the GRD system.',
}

export default function AdminDashboardLayout({ children }) {
  return children;
} 